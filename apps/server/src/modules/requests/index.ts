import { RequestsDefinitions } from "./definitions.requests";
import { RequestsResolvers } from "./resolvers.requests";

export const RequestsModule = {
  resolvers: RequestsResolvers,
  typeDefs: RequestsDefinitions,
};
