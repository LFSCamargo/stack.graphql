import { CardUserResolvers } from "./resolvers.cardUser";
import { CardUserDefinitions } from "./definitions.cardUser";

export const CardUserModule = {
  resolvers: CardUserResolvers,
  typeDefs: CardUserDefinitions,
};
