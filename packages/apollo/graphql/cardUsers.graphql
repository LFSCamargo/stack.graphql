query CardUsers($input: PaginationInput!) {
  cardUsers(input: $input) {
    count
    pageInfo {
      hasNextPage
    }
    data {
      cardNumber
      name
      _id
      newAccount
      balance
    }
  }
}
